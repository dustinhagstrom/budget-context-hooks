import Header from "./header";
import Inputs from "./inputs";
import Lists from "./lists"; //if index.js exists in a folder then this
//import is smart enough to know where the base file is actually located
//in the file tree

export { Header, Inputs, Lists };
